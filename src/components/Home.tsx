import React from 'react'

const Home = () => {
  return (
    <div className='container big-margin-center'>
      Read more about how to adapt styling based on props in the Adapting based
      on props section. The properties that are passed into an interpolated
      function get attached a special property, theme, which is injected by a
      higher level ThemeProvider component. Check the section on
    </div>
  )
}

export default Home
